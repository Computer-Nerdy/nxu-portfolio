# 📦 3D Models Directory

Place your custom 3D model files here:
- Recommended format: `.glb` (Binary glTF - fast & self-contained with embedded textures)
- Target file path: `models/esp32.glb`

The website's Three.js loader is configured to automatically inspect components and render interactive labels!
